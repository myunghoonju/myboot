{{>layout/header}}
<div class="col-md-12">
    <div class="row">
        <div class="col-md-6">
            Good Day
            {{#name}}
               {{name}} !
                <br>
                <a href="/post/save" role="button" class="btn btn-primary">new content</a>
                <a href="/logout" class="btn btn-info active" role="button">Log-out</a>
            {{/name}}
            {{^name}}
                <br>
            <a href="/oauth2/authorization/google" class="btn btn-success active" role="button">Google Login</a>
            {{/name}}
        </div>
    </div>
    {{#name}}
    <br>
    <table class="table table-horizontal table-bordered">
        <thead class="thead-strong">
        <tr>
            <th>게시글번호</th>
            <th>제목</th>
            <th>작성자</th>
            <th>최종수정일</th>
        </tr>
        </thead>
        <tbody id="tbody">
        {{#posts}}
            <tr>
                <td>{{id}}</td>
                <td><a href="/post/update/{{id}}">{{title}}</a></td>
                <td>{{author}}</td>
                <td>{{modifiedDate}}</td>
            </tr>
        {{/posts}}
        </tbody>
    </table>
    {{/name}}
</div>
{{>layout/footer}}